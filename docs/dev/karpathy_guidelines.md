# 코딩 작업 지침 (Karpathy Guidelines)

> **위상**: 작업 지침(비규범). `CLAUDE.md §2`(불변식)와 충돌하면 **CLAUDE.md가 이긴다.**
> 다만 이 저장소에서 코드를 쓰고 고칠 때의 **기본 태도**로 따른다 — 사람이든 에이전트든,
> 최상위 세션이든 서브에이전트든 동일하게 적용된다.
>
> **출처**: [forrestchang/andrej-karpathy-skills](https://github.com/forrestchang/andrej-karpathy-skills)
> (MIT License)의 `CLAUDE.md`를 번역했다. 원안은 LLM 코딩의 반복되는 실패 패턴에 대한
> [Andrej Karpathy의 관찰](https://x.com/karpathy/status/2015883857489522876)에서 왔다.

**트레이드오프**: 이 지침은 속도보다 **신중함** 쪽으로 기운다. 사소한 작업에는 재량껏 판단한다.

---

## 1. 코딩 전에 생각한다

**추측하지 않는다. 혼란을 감추지 않는다. 트레이드오프를 드러낸다.**

구현에 들어가기 전에:

- 전제를 명시적으로 말한다. 불확실하면 묻는다.
- 해석이 여러 갈래면 전부 제시한다 — 말없이 하나를 고르지 않는다.
- 더 단순한 방법이 있으면 말한다. 근거가 있으면 반대 의견을 낸다.
- 불명확한 것이 있으면 **멈춘다.** 무엇이 헷갈리는지 이름 붙이고 묻는다.

## 2. 단순함이 먼저다

**문제를 푸는 최소한의 코드. 추측성 코드는 없다.**

- 요청받지 않은 기능은 만들지 않는다.
- 한 번만 쓰는 코드에 추상화를 씌우지 않는다.
- 요청받지 않은 "유연성"·"설정 가능성"을 넣지 않는다.
- 일어날 수 없는 상황에 대한 오류 처리를 넣지 않는다.
- 200줄을 썼는데 50줄로 되는 일이면, 다시 쓴다.

스스로 물을 것: **"시니어 엔지니어가 이걸 보고 과하다고 할까?"** 그렇다면 단순화한다.

## 3. 외과적으로 고친다

**꼭 건드려야 하는 것만 건드린다. 치우는 것은 내가 어지른 것뿐이다.**

기존 코드를 수정할 때:

- 인접한 코드·주석·포매팅을 "개선"하지 않는다.
- 망가지지 않은 것을 리팩토링하지 않는다.
- 내 취향과 다르더라도 **기존 스타일에 맞춘다.**
- 무관한 죽은 코드를 발견하면 **말은 하되 지우지 않는다.**

내 변경이 고아를 만들었을 때:

- **내 변경 때문에** 안 쓰이게 된 import·변수·함수는 제거한다.
- 원래부터 죽어 있던 코드는 요청받지 않는 한 건드리지 않는다.

판정 기준: **바뀐 모든 줄이 사용자의 요청으로 직접 추적되어야 한다.**

## 4. 목표 기반으로 실행한다

**성공 기준을 정의하고, 검증될 때까지 돈다.**

작업을 검증 가능한 목표로 바꾼다:

- "검증 로직 추가" → "잘못된 입력에 대한 테스트를 쓰고, 통과시킨다"
- "버그 수정" → "버그를 재현하는 테스트를 쓰고, 통과시킨다"
- "X 리팩토링" → "리팩토링 전후로 테스트가 통과하는지 보장한다"

여러 단계짜리 작업은 짧은 계획을 먼저 말한다:

```
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
3. [단계] → 검증: [확인 방법]
```

성공 기준이 강하면 스스로 루프를 돌 수 있다. 기준이 약하면("돌아가게 해줘") 계속 되묻게 된다.

---

**이 지침이 작동하고 있다는 신호**: diff에 불필요한 변경이 줄어들고, 과설계 때문에 다시 쓰는 일이
줄어들며, 명확화 질문이 **실수를 저지른 뒤가 아니라 구현 전에** 나온다.

---

## 이 저장소에서의 적용 메모

- **3장(외과적 수정)은 `docs/spec/`·`SPEC.md` 동기화 의무(§2-2)와 충돌하지 않는다** — 계약이
  바뀌는 변경에서 위임 문서를 같이 고치는 것은 "무관한 개선"이 아니라 요청의 일부다.
- **1장(전제 명시)은 §2-3 "정직한 분모" 원칙과 같은 방향이다** — 모르는 것을 아는 척 채우지 않고
  `PK_UNKNOWN`·`UNKNOWN`으로 남기는 것이 이 저장소의 기본값이다.
- **4장(검증 기준)의 이 저장소 표준 형태**는 `pytest` green + 해당 Phase의 `scripts/verify_phase*_real.py`
  FAIL 0이다(→ `docs/guide/commands.md`).
